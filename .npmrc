; 本工作区专用的 npm 设置(故意使用本机绝对路径)。
;
; 为什么把 npm 缓存放在工作区内:
;   1) 让「在沙箱里执行的安装命令」也能成功,不必每次提权;
;   2) 项目自包含,删除工作区即清空缓存;
;   3) 不污染 D:\Program Files 下的全局缓存。
; .cache/ 已在 .gitignore 中忽略。
;
; 注:pnpm 的设置(store-dir / cache-dir)不从这个文件读 —— pnpm 11 已把这类设置
; 迁到 pnpm-workspace.yaml,实测写在 .npmrc 里会被忽略(还会让 npm 报
; "Unknown project config" 警告)。本工作区 pnpm 的 store 默认就落在
; D:\ProgramData\SynthFlow\.pnpm-store,无需额外配置。

registry=https://registry.npmmirror.com
cache=D:\ProgramData\SynthFlow\.cache\npm
