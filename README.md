# SynthFlow · 预生成式 AI 编程工作台

> 你还在打字，代码已经在写了。
> 不是"输入 → 点发送 → 等回复"，而是**边说边生成**：AI 与你的输入同步思考、同步产出、同步给建议，你随时改前文，它只做增量。

---

## 30 秒上手

```bat
:: 双击项目根目录的 start.cmd，或者：
cd /d D:\ProgramData\SynthFlow
npm start
```

浏览器打开 **http://127.0.0.1:7788/** 即可。模型在右上角「⚙ 设置」里配置（DeepSeek / OpenAI 兼容 / 本地 Ollama 都行），
Key 只写进本机 `.synthflow/config.json`，不会外发。

想看它自己证明自己没问题：

```bat
npm run smoke        :: 60 项离线冒烟测试（不联网、不花钱）
node scripts/livecheck.mjs   :: 对正在运行的服务做一次真实用户流程演练（会真实调用你配置的模型）
```

> 需要零成本离线演练时：`node src/server.js --provider mock`（内置演示模型，界面上已隐藏，仅供开发与测试）。

---

## 它到底哪里不一样

| 传统 AI 编程（Copilot / Cursor Tab / Trae / 各类 Agent） | SynthFlow |
| --- | --- |
| 敲代码后提示几行，按 Tab 采纳 | 敲**需求**时就流式生成整个功能 |
| 一句话提交 → 等回复 → 看结果 | 不提交、不等待，打字即生成，全程不中断 |
| 生成时你只能看着 | 生成的同时并行思考，主动推可采纳的建议给你 |
| 一次对话改一个文件 | 自动建目录/多文件，改动实时跳转到对应代码 |
| 改前文 = 推倒重来 | 自动判断：增量补丁 / 增量续写 / 才重生成 |
| 想撤销靠手写 git | 一键回退到"这句话还没输入时"的版本 |

四个真正稀缺的能力，SynthFlow 都做了实现：

1. **预演（Speculation）**：你停顿 0.3 秒，它就开始生成，但**先不落盘**。等你被判定"写完了"，直接**采纳预演结果**——
   省掉一次完整模型调用（省钱 + 更快）。如果你在预演期间又改了需求，它会丢掉过期结果、改走增量。
2. **意图完整度判定**：不是简单 debounce。用 10+ 个信号（句末标点、连接词结尾、括号/围栏闭合、动作词、打字停顿、
   增长趋势…）算出一个 0~1 的分数，界面上**实时显示**它为什么认为你还没写完。停手不打了还会兜底开工。
3. **上下文锚定式增量**：改前文中间的一段时，它把"改掉了什么 / 新要求是什么"变成定点补丁指令，
   并把最近碰过的文件作为锚点一并塞进上下文，明确要求模型**只动受影响的文件**。
4. **版本链 + 一键回退**：每一轮生成前后各打一次全量快照，"回退一步"同时回滚**文件**和**提示词**。

---

## 你提的 18 条，逐条对照

| # | 你的要求 | 实现位置 | 状态 |
| --- | --- | --- | --- |
| 1 | 写 prompt 时同步流式生成代码，无需点发送、流程不中断 | `runner.js#onInput` → 预演/提交双定时器；前端 `app.js` 持续上报输入 | ✅ |
| 2 | 生成同时并行思考，主动输出需求补全/优化/风险建议，采纳后自动写入 prompt | 流协议 `think` / `suggest` 双通道；`session.adoptSuggestion()` 写回输入框 | ✅ |
| 3 | 作用于项目、自动建目录文件、多文件修改、优化时自动跳到对应文件 | `workspace.js` 沙箱 + `file:start` 事件触发前端自动切标签页与差异条 | ✅ |
| 4 | 删改输入框时判断"增量改"还是"重头生成"；prompt 很长时不重生成 | `session.decidePromptChange()`，代码量 > 24KB 时自动收紧重生成阈值 | ✅ |
| 5 | 一次回退到"这句话没输入时"的版本 | `session.rollback()` + 快照链，按钮 / 时间线 / `Alt+Z` | ✅ |
| 6 | 判断一句话是否输入完了，不能刚打一个字就改代码 | `session.analyzeIntent()` 多信号打分 + 停顿兜底 | ✅ |
| 7 | 中途修改前文，自动定位上下文片段做增量更新 | `changedSpan()` 定位 + 锚点文件 + `incremental` 模式指令 | ✅ |
| 8 | 维护会话级完整上下文栈，自动拼接成完整 Prompt | `session.segments` + `compilePrompt()` + `/api/context` | ✅ |
| 9 | 生成中继续输入 → 增量续写而非推倒重写 | `onInput` 的 busy 分支置 `pendingInput`，本轮结束后 `#maybeResume()` 自动续写 | ✅ |
| 10 | 生成过程中发现更优方向就自动提出，并同步优化 prompt | `suggest(kind=optimize)` 实时推送；可开「自动采纳高价值优化」开关 | ✅ |
| 11 | 后续可挂 RAG 和 skills，熟悉用户习惯 | `rag.js`（BM25 + `.synthflow/skills/*.md`）+ `memory.js` 用户画像注入 | ✅ 已内置 |
| 12 | 参考 GitHub 上类似开源仓库 | 见 `docs/RESEARCH.md`（Aider / Continue / Cline / Claude Code 等借鉴点） | ✅ |
| 13 | 自动保存 prompt 对话历史，学习用户操作习惯 | `.synthflow/history/`、`.synthflow/memory/profile.json`、快捷片段 chips | ✅ |
| 14 | 无技术栈要求 | 后端零依赖（Node 内置模块），前端原生 HTML/CSS/JS，**零 npm install** | ✅ |
| 15 | 全权交给你，睡醒要有成品 | 本仓库就是成品，含测试与文档 | ✅ |
| 16 | 别弄坏电脑，交代清楚下载了什么、放哪了 | **没有下载任何东西、没有装任何依赖**，全部产物都在 `D:\ProgramData\SynthFlow` 内，见「磁盘与进程」 | ✅ |
| 17 | 自己冒烟测试、上网看别人方案、注意 token 余额 | 60 项离线冒烟 + 在线验收脚本；`docs/RESEARCH.md`；状态栏实时显示 token 用量 | ✅ |
| 18 | 权限问题找别的办法，太麻烦就先停下 | 全程无阻塞：服务只监听 `127.0.0.1`，无需管理员权限 | ✅ |

---

## v2 更新（你验收后提的 10 条 + 4 个附加项）

| # | 你的反馈 | 处理 |
| --- | --- | --- |
| 1 | 差异展示不好，下方有大片黑色无法操作区域 | **根因是 bug**：`.diff-strip` 的 `max-height:30%` 在 grid `auto` 行里无法解析、退化成按内容无限高，把代码区挤没了。现在**彻底删掉底部差异条**，改成代码区一键切换「完整代码 ⇄ 本轮差异」（`Ctrl+D`），未改动区域自动折叠成 `⋯ 折叠 N 行` | ✅ |
| 2 | 思考栏定位不清，翻上下文容易串在一起 | 每轮生成变成**独立轮次块**：`第 N 轮 · 模式 · 时间 · 文件数 · 耗时`，左侧色条区分预演/正式；右上角 ↑↓ 跳转轮次并高亮闪烁；点轮次头可整块折叠 | ✅ |
| 3 | 思考内容可折叠、默认折叠 | 思考块**默认折叠**（流式时自动展开、写完后自动收起），标题显示首句摘要与字数；面板右上「思考：折叠/展开」全局切换，偏好记在 localStorage | ✅ |
| 4 | 右下角黄色提示框太多 | 改为**底部状态提示栏**：`输入中 / 预演中 / 正在生成 / 正在写入 / 已写入 N 个文件 · 耗时 / 出错了`；Toast 只保留需要你决策或出错的（补丁未命中、模型未就绪、版本分支丢弃），并做了去重 + 最多 3 条 | ✅ |
| 5 | 删掉预设展示模型，用真实 Key 测试 | 界面上**已移除**内置演示模型，默认指向真实服务商；它只在 `node src/server.js --provider mock` 下可用（供离线回归测试）。**已用你的 DeepSeek Key 完成真实端到端验收** | ✅ |
| 6 | 优化/风险建议应在本轮完成后再弹出 | 正式生成阶段的建议**本轮落盘后统一推送**并按影响度排序（high 在前）；预演阶段你还在打字、没有"本轮"概念，仍实时给 | ✅ |
| 7 | 采纳后生成了新一轮，此时还应能继续提建议 | 提示词里加了**硬性要求**：无论全新生成还是增量补丁，每轮都必须基于刚改的代码再给 2 条建议；采纳后的新一轮实测又给出 3 条 | ✅ |
| 8 | 预演延迟调高，别逐字判断 | 预演延迟 `320ms → 1000ms`；前端上报节流 `180ms → 300ms`；服务端对意图事件做 `90ms` 合并；判定条过渡改为 0.3s 缓动。**结果是：完整句子直接生成（跳过预演，省一次调用），半截需求停顿时才预演** | ✅ |
| 9 | 回退后还要能返回去（v17→v16→v17） | 版本链从"数组=历史"改成**历史 + 游标**：回退不再删除版本，新增 `⏩ 前进`（`Alt+Shift+Z`）、时间线可点击跳转任意版本；在历史版本上继续生成会**丢弃右侧分支并明确提示**（与 git 一致） | ✅ |
| 10 | 白天/黑夜主题切换 | 顶栏 🌗 按钮三态循环：**跟随系统 → 亮色 → 暗色**，记住你的选择；浅色主题重调了全部令牌（背景层次、边框、文本、强调色、语法高亮、滚动条、阴影），不是简单反色 | ✅ |
| +1 | 补丁未命中自动重试一次 | 未命中时把**真实文件内容**重新喂回去让它重出补丁（只重试 1 次，可在设置里关闭）。冒烟测试用桩 provider 强制失败验证过；真实模型实测**一次命中** | ✅ |
| +2 | 本轮新增行标绿点 | 增量补丁命中的新增行在代码区左侧标绿点（整文件新建/重写不标，避免刷屏）；高亮器改成**逐行输出**以保证行号不错位 | ✅ |
| +3 | 状态栏显示 token 用量 | 状态栏右侧显示 `本轮 ≈N tokens · 累计 M 次调用 · X tokens`；接真实模型时优先使用接口返回的**真实 usage**（已开启 `stream_options.include_usage`），否则按字符数估算 | ✅ |
| +4 | 键盘流 | `Alt+1..9` 采纳第 N 条建议、`Esc` 停止生成、`Ctrl+P` 文件快速跳转面板、`Ctrl+D` 代码/差异切换、`Alt+Z` / `Alt+Shift+Z` 回退/前进、`Alt+Enter` 立即生成。顶栏 `?` 有完整速查 | ✅ |

---

## 界面速览

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ ◆ SynthFlow [模型就绪] [运行状态]      ⏪回退 ⏩前进 ↻重生成 🌗主题 ⚙设置     │
├────────────┬──────────────────────────────────────────┬──────────────────────┤
│ 项目文件    │ 标签页                       [代码|本轮差异] 保存             │
│  src/       │  ┌────────────────────────────────────┐  │ 思考 & 建议  3/7 ↑↓ │
│   index.html│  │ 12 │ 代码在这里实时长出来            │  │  ┌────────────────┐ │
│   main.js  ●│  │ 13 │ 新增行左侧标绿点 ●              │  │  │▸ 第 3 轮·增量··│ │
│   styles.css│  └────────────────────────────────────┘  │  │  ▸ 思考 · 摘要… │ │
│ 版本时间线  │                                          │  │  ⚠ 风险提示 采纳│ │
│ v0 v1 ②v2   │                                          │  │  ✦ 优化方向 采纳│ │
├────────────┴──────────────────────────────────────────┴──────────────────────┤
│ 意图判定 ████████░░ 78% 还在写      策略：增量续写 · 变动 12%                  │
│ ┌───────────────────────────────────────────────┐ ┌────────────────────────┐ │
│ │ 直接开始打字…（停顿约 1 秒我就开始预演）        │ │ 立即生成 / 停止生成     │ │
│ └───────────────────────────────────────────────┘ │ [用中文注释][加错误处理] │ │
│ ● 已写入 3 个文件  810ms · 版本 v5     本轮≈2.8k · 累计5次·9.1k tokens   0 字 │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 值得亲手试一遍的四件事

1. **只打半句就停**：输入 `帮我写一个后台管理系统，包含` → 别动。1 秒后右侧开始流出思考、代码开始在编辑器里长出来，
   但左边**没有产生版本**——因为它在预演。补上 `用户列表、搜索和分页。`，它会直接把预演结果落盘（提示"预演结果被直接采纳"，省掉一次调用）。
2. **改前文**：把提示词里的 `蓝色主题` 改成 `暗色主题`，底部状态栏显示"策略：定点增量补丁"，右侧只出现涉及文件的补丁块，
   其它文件字节级不变。
3. **`Alt+Z` 回退，再 `Alt+Shift+Z` 前进**：文件与提示词一起回到上一句话之前，还能原样回来。
4. **`Ctrl+D`**：在"完整代码"和"本轮差异"之间切换，差异只显示真正变化的行。

---

## 接真实模型

三种方式任选（优先级：环境变量 > 界面设置 > 配置文件）：

**A. 界面（推荐）**：右上角「⚙ 设置」→ 选服务商 → 填模型名 / Base URL / API Key → 保存并生效。
Key 只写入本机 `D:\ProgramData\SynthFlow\.synthflow\config.json`，不会外发到任何地方。

**B. 环境变量**（适合写进脚本）：

```bat
set SYNTHFLOW_PROVIDER=deepseek
set SYNTHFLOW_API_KEY=sk-你的key
set SYNTHFLOW_MODEL=deepseek-chat
npm start
```

**C. 配置文件** `.synthflow/config.json`：

```json
{
  "provider": "deepseek",
  "baseUrl": "https://api.deepseek.com/v1",
  "model": "deepseek-chat",
  "temperature": 0.3,
  "maxTokens": 4096,
  "specDelayMs": 1000,
  "commitIdleMs": 900,
  "settleMs": 1600,
  "intentThreshold": 0.6,
  "autoCommit": true,
  "autoAdoptHigh": false,
  "patchRetry": true
}
```

预设：`deepseek` · `openai` · `ollama`（本地模型，不花钱）· `siliconflow` · `custom`（任意 OpenAI 兼容端点）。
内置演示模型已从界面移除，仅在 `--provider mock` 时可用（给开发和测试用）。

### 省钱建议（这几条很重要）

- **预演采纳机制本身就在省钱**：完整句子直接生成（跳过预演）；半截需求停顿后预演，判定写完时**直接采纳预演结果**，
  一次输入通常只调用模型 **1 次**，而不是"预演 + 提交"2 次。
- **状态栏实时显示用量**：`本轮 ≈N tokens · 累计 M 次调用 · X tokens`。接真实模型时优先使用接口返回的**真实 usage**，
  不是估算值——你能直接看到每次改动花了多少。
- **预演延迟**默认 1000ms。调大（如 1500ms）会进一步减少打字过程中的调用；调小会更"跟手"但更费。
- **补丁未命中自动重试**默认开启，会多消耗一次调用；余额紧张时可以关掉。
- 想彻底零成本地跑真实模型：装 Ollama，`provider` 填 `ollama` + `qwen2.5-coder:7b`。
- 每次生成的耗时、token 数都记进 `.synthflow/memory/profile.json`，可以自己核对。

---

## 目录结构与磁盘去向

```
D:\ProgramData\SynthFlow\
├── start.cmd              一键启动（双击即可，会打开浏览器）
├── package.json           零依赖，无 node_modules
├── src\                   后端（全部 Node 内置模块）
│   ├── server.js          HTTP + SSE + REST
│   ├── runner.js          预演/提交/落盘/回退 的调度核心
│   ├── session.js         上下文栈 · 意图判定 · 漂移决策 · 版本链
│   ├── workspace.js       沙箱文件读写 · 补丁命中 · 快照
│   ├── protocol.js        流协议解析（think/suggest/file/memory）
│   ├── prompt.js          上下文组装（项目地图/RAG/技能/用户画像）
│   ├── llm.js             模型接入层 + 内置离线演示大脑（界面已隐藏）
│   ├── rag.js             BM25 检索 + skills
│   └── memory.js          习惯记忆与用户画像
├── public\                前端（原生，无构建）
│   ├── index.html  styles.css  app.js
├── scripts\
│   ├── smoke.mjs          60 项离线冒烟测试
│   ├── livecheck.mjs      在线端到端验收
│   └── clean.mjs          磁盘清理（默认只看不删）
├── workspace\             ★ 生成出来的代码全在这里（可以被你随意删）
├── .synthflow\            ★ 本机运行数据（可以被你随意删）
│   ├── config.json        你的模型配置（clean 脚本永不删它）
│   ├── snapshots\         版本快照（回退用）
│   ├── sessions\          会话上下文栈
│   ├── history\ memory\   提示词历史与习惯画像
│   ├── skills\            放 *.md 即可被自动挂载为技能
│   ├── index.json         RAG 索引（可自动重建）
│   └── testrun\           冒烟测试产物（跑完可删）
└── docs\                  设计文档与调研笔记
```

**下载了什么？——什么都没下载。** 没有 `npm install`、没有 `node_modules`、没有全局安装、没有改注册表、
没有写 `%USERPROFILE%` 或系统临时目录。整个项目就是上面的文本文件，加起来几 MB。
npm 缓存指向项目内的 `.cache\npm`（原仓库已有配置），本次未被使用。

### 磁盘占用与清理

```bat
node scripts/clean.mjs              :: 只看占用，不删任何东西
node scripts/clean.mjs --test       :: 删冒烟测试产物
node scripts/clean.mjs --workspace  :: 清空生成出来的代码
node scripts/clean.mjs --history    :: 删提示词历史与记忆
node scripts/clean.mjs --snapshots  :: 删版本快照（回退能力重置）
node scripts/clean.mjs --all        :: 以上全部（保留 config.json）
```

用完后想彻底清空内存占用：关掉 `start.cmd` 那个黑窗口即可（或 `Ctrl+C`）。服务只监听 `127.0.0.1`，不对外网开放。

---

## 它是怎么工作的（简版）

```
你打字  ──每 300ms 上报──▶  POST /api/input
                              │
                     analyzeIntent()   ──▶  SSE: intent（界面实时显示判定理由，服务端 90ms 合并）
                     decidePromptChange() ─▶  增量 / 续写 / 重生成
                              │
              ┌───────────────┴────────────────┐
    ~1000ms 后预演                   判定完成后 900ms 提交
    （不落盘，可被丢弃）              （优先直接采纳预演结果）
              │                                │
              └──────────► 模型流式输出 ◄───────┘
                              │
        <<<SF think>>>  <<<SF suggest>>>  <<<SF file ...>>>
                              │
                   增量解析器边收边推 SSE（高频 delta 45ms 合并）
                              │
        前端：轮次块 / 可折叠思考 / 代码逐行高亮+新增行绿点 / 自动跳转文件
                              │
                  提交时：Δ文件 → 沙箱补丁 → 快照 → 版本游标前移
                              │
              补丁未命中 → 把真实文件内容喂回去，自动重试一次
```

详细设计（协议语法、判定公式、并发与取消、版本游标、错误恢复）见 **`docs/DESIGN.md`**；
开源方案借鉴与差异见 **`docs/RESEARCH.md`**。

---

## 验证记录（本次交付实测）

```
npm run smoke                 60 通过 / 0 失败 · 13.0s
node scripts/livecheck.mjs    全部通过（离线内置模型 · 11.9s）
node scripts/livecheck.mjs    全部通过（真实 DeepSeek · 38.0s）
  场景 A 边打字边生成  → 中途停顿触发"预演"；本轮结束给出 3 条建议；
                        预演结果被直接采纳（省一次调用）；返回真实 token 用量 ≈2811
  场景 B 采纳建议      → 提示词 +69 字，自动跟进生成，并**又给出 3 条新建议**
  场景 C 追加需求      → mode=continue，补丁一次命中，未涉及文件字节级不变
  场景 D 回退 + 前进   → v3 ⇄ v4 往返：文件、提示词、游标同步；已在最新版时正确拒绝
  场景 E 差异与检索    → 紧凑差异 34 段含 2 处折叠、索引 5 片段、版本链 v0 → v1 → v2 → v3 → v4
```

代价：整轮真实验收约 5 次模型调用、合计约 1.4 万 token。**冒烟测试全程零成本**（不联网）。

---

## 已知边界（不吹牛）

- **内置演示模型**只会按关键词产出几类模板（后台管理 / 登录 / 图表 / 表单 / 通用小工具）。
  界面上已移除，只用于离线回归测试；真实代码质量取决于你接的模型。
- **补丁未命中**时会明确提示，并**自动重试一次**（把真实文件内容重新喂回去）；仍失败则保留其它文件的改动，
  不会静默改坏文件。兜底策略是三层：精确 → 忽略首尾空白/行归一 → 首行锚点。
- **版本链是线性的**：可以来回回退/前进，但在历史版本上继续生成会**丢弃右侧分支**（会提示你丢弃了几个版本），
  与 git 在历史提交上继续提交的行为一致。暂不支持分支树。
- **RAG 是词法检索（BM25）**，不是向量检索：零依赖、零下载、够用，但对语义改写不敏感。
- **思考折叠是本地偏好**（localStorage），换浏览器会回到默认的"折叠"。
- 前端目前是**工作台形态**（浏览器内），不是 VS Code 插件。插件化需要额外的 IDE 宿主环境配合。

## 下一步可以做什么

1. 接 VS Code 扩展（把 `workspace/` 换成 IDE 的文档模型，`file:start` 事件映射成编辑器跳转）。
2. 把 BM25 换成向量检索（`rag.js` 已经是可替换的单点）。
3. 版本树分支（现在丢弃的分支可以留下来，支持"从 v3 再试一版"并对比两条分支的代码）。
4. 让建议支持"部分采纳"（只取 insert 的某一句）。
5. skills 目录已经能挂 `.md`，可以往里塞你们团队的代码规范、组件库用法。
6. 差异视图支持"与任意历史版本对比"（现在是只看本轮差异，快照里其实有全部版本）。

---

MIT License · 生成物与运行数据都在本仓库内，随时可删。
