feat(model): 可选模型（flash / pro）+ 推理强度，并修掉思维链被当成代码的隐患

用户反馈：模型应该能自己选，比如 DeepSeek 可以选 flash 还是 pro，
并且有对应的推理能力强度选择。

参数不照文档猜，先用真实 Key 实测（结论写进代码注释）：

  GET  /v1/models                  -> 只有 deepseek-flash 与 deepseek-v4-pro
  POST reasoning_effort='ultra'    -> 422，错误信息给出合法枚举
                                      none|minimal|low|medium|high|xhigh|max
  POST thinking:{type:'enabled'} + reasoning_effort
                                   -> 200，响应多出 reasoning_content
                                      与 completion_tokens_details.reasoning_tokens
  POST thinking:{type:'disabled'}  -> 200，无 reasoning_content
  只给 reasoning_effort 不给 thinking -> 200（思考默认就是开的，只有"关"需要显式发）
  POST model='deepseek-chat'       -> 200，但回显 model=deepseek-flash 且没有思维链

最后一条最关键：旧模型名还能用，却被静默转成 flash 的非思考模式 ——
界面上一切正常，只是推理强度怎么调都不生效。参数被静默接受但无效果，比直接报错更难发现。

做了什么：

1) MODEL_CATALOG：按服务商给出模型清单与强度选项（DeepSeek 为实测值）。
   设置面板的模型框改成 input+datalist：能下拉选，也能手填；没实测过的服务商不给清单，
   但绝不因此让用户没法输入模型名。

2) 推理强度按服务商翻译成请求字段：
   DeepSeek 关闭 -> thinking:{type:'disabled'}；低/中/高/最高 -> reasoning_effort。
   默认"低"而不是官方默认的 high —— 预演由打字停顿触发、一句话可能触发好几次，
   默认开高档会把钱烧在没写完的半句话上。

3) 旧模型名迁移：deepseek-chat/reasoner/coder -> deepseek-flash，并补齐强度字段。
   迁移要在 { 默认值, 存储值 } 合并之后再做一遍，否则会被存储值覆盖
   （第一版就踩了这个，启动后仍显示 deepseek-chat）。结果会落盘，避免 config.json 里
   一直躺着已下线的名字。

4) 思维链分流：原来 delta.content ?? delta.reasoning_content 把 CoT 和正文挤进同一条流。
   这在 deepseek-chat（非思考）下正常，一旦切到 flash/pro（思考是默认行为），
   模型的思考过程会被协议解析器当成真的文件操作。现在 reasoning_content 走
   应用本来就有的 think:start/think:delta/think:end 通道。

5) 端点不认 reasoning_effort 时（400/422）自动去掉该参数重试一次，并给出说明。

6) 顶栏徽标显示 DeepSeek · deepseek-flash · 思考·高；状态栏显示
   本轮约 1.9k（思考 403）—— 思维链单独计费，得让用户看见钱花在哪。

测试：冒烟 84->89（模型清单纯度、强度映射、思维链隔离（往 CoT 里投毒一段合法协议标记，
断言一个字都不能漏进正文流）、422 降级重试、provider 信息 5 条）。
UI 62->65（模型下拉+手填、顶栏徽标、改强度会真写进配置）。
猴子测试 2 种子 x 150 步 0 违规。

真模型实测（强度=高）：思考栏收到 1902 字真实思维链，用量显示"思考 403"，
项目目录只有目标文件、没有可疑文件。
